# Update

Check for new trainsh releases.

## When to use it

- See whether the installed version is behind.

## Command

```bash
train update --help
```

## CLI help output

```text
[--help] [--check]

Check for updates and automatically upgrade tmux-trainsh.

Options:
  --check    Only check for updates, don't install

Examples:
  train update          # Check and install update
  train update --check  # Only check, show update command
```
